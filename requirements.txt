gradio
requests
spaces
# ── Agent framework & APIs ─────────────────────────────────────────
smolagents>=1.26
openai>=1.68
requests>=2.32
python-dotenv>=1.0

# ── Web search (used by smolagents DuckDuckGoSearchTool) ───────────
ddgs

# ── Local audio & video (ticket 02) ────────────────────────────────
# Local audio transcription (transcribe_audio) — lightweight model, WHISPER_MODEL
faster-whisper>=1.0
# Video frame sampling (extract_video_frames). NOTE: also requires the
# ffmpeg/ffprobe binaries on PATH (brew install ffmpeg / apt install ffmpeg).
yt-dlp

# ── Data & document parsing ────────────────────────────────────────
pandas>=2.2
openpyxl>=3.1
xlrd>=2.0.1
pdfplumber>=0.11
python-docx>=1.1
python-pptx>=1.0
youtube-transcript-api>=1.0
Pillow>=10.0
pytesseract>=0.3

# ── Local vision model (inspect_image → Qwen2.5-VL via transformers) ──
torch
# torchvision is required by transformers v5 for image processing
torchvision
transformers
accelerate

# ── Libraries the agent may import in generated code ───────────────
# (mirrors additional_authorized_imports in text.py)
numpy
scipy
matplotlib
seaborn
sympy
scikit-learn
opencv-python
pypdf
PyMuPDF
beautifulsoup4>=4.12
lxml